prompt 1 - 

Design a production-ready architecture for an investigative social media dashboard.
Use React (Vite) for frontend, Node.js + Express for backend, Neo4j as graph database, and a Python FastAPI service for ML tasks.
Show how data flows between ingestion → Neo4j → API → frontend → chatbot (RAG).
Keep it simple and developer-friendly.

prompt 2 - 

Propose FAST API endpoints for an investigative dashboard backend.
Include stats, time-series, top contributors, subgraph fetching, and chatbot endpoints.
For the graph endpoint, return JSONL


prompt 3 - 

Design a simple RAG pipeline for social media analysis:
Semantic search over post embeddings
Select top-k relevant posts
Send condensed context to LLM
Return answer with cited sources
Explain how to manage token limits.
